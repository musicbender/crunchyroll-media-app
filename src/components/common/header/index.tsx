import { FC } from 'react';
import { HeaderWrapper } from './styles';

const Header: FC = () => {
  return (
    <HeaderWrapper>
      <h1>Media Content</h1>
    </HeaderWrapper>
  );
};

export default Header;
